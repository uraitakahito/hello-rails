
## How to start

Type:

```console
% bundle install
% bin/rails db:create
% bin/rails db:migrate RAILS_ENV=development
% bin/rails server -p 3000
% bin/rails dev:cache
Development mode is now being cached.
```

### CAUTION ###

Initial `BUNDLE_APP_CONFIG` is `/usr/local/bundle`
